// General Settings //

// Experimental
sv_allow_point_servercommand always

hostname "The Youkai Pound | New Jersey | 18+ (Unless?)"

sv_forcepreload "1"                              // forces server to preload assets?

con_timestamp "1" // timestamps console log

//Experimental ddos mitigation
sv_max_queries_sec 1 
sv_max_queries_window 60
sv_max_queries_sec_global 200

//Advertisements
sm_advertisements_interval 960
sm_advertisements_random 1

//vote options
sv_allow_votes 0

// Re-use parachute, parachute floats upwards while on fire
sm_cvar tf_parachute_deploy_toggle_allowed 1
sm_cvar tf_parachute_maxspeed_onfire_z 100.0

sm_cvar tf_max_voice_speak_delay "-1"
//remove voice spam limit

// Set to lock per-frame time elapse
host_framerate 0

// Player collision disable
tf_avoidteammates_pushaway 0

// Steam Group ID
sv_steamgroup 45503416

//Enables explosive jumping while unable to move
tf_preround_push_from_damage_enable 1

// Misc
sv_downloadurl "http://fastdl.kogasa.tf/tf2"
sv_tags "youkai,touhou,kogasatopia,bant,uncletopia,uncle,un,pound,kogasa,whale"

// Control where the client gets content from 
// 0 = anywhere, 1 = anywhere listed in white list, 2 = steam official content only
sv_pure 0

// Is the server pausable
sv_pausable 0

// Type of server 0=internet 1=lan
sv_lan 0

// Collect CPU usage stats
sv_stats 1

// Contact & Region //

// Contact email for server
sv_contact malkuthbr2@pissmail.com

// The region of the world to report this server in.
// -1 is the world, 0 is USA east coast, 1 is USA west coast
// 2 south america, 3 europe, 4 asia, 5 australia, 6 middle east, 7 africa
sv_region 0



// Rcon Settings //

// Password for rcon authentication (Remote CONtrol)
rcon_password jew

// Number of minutes to ban users who fail rcon authentication
sv_rcon_banpenalty 1440

// Max number of times a user can fail rcon authentication before being banned
sv_rcon_maxfailures 5



// Log Settings //

// Enables logging to file, console, and udp < on | off >.
log on

// Log server information to only one file.
sv_log_onefile 0

// Log server information in the log file.
sv_logfile 1

// Log server bans in the server logs.
sv_logbans 1

// Echo log information to the console.
sv_logecho 0

// ==== Interpolation ====
sv_client_min_interp_ratio 1        // Players must use at least 1 interp ratio (good for consistency)
sv_client_max_interp_ratio 2        // Prevents abuse of high interp ratios like 5
// ==== Rates ====
sv_minrate 80000                    // Minimum client bandwidth; stable for 24–32 players
sv_maxrate 0                        // Unlimited; engine auto-clamps safely
sv_minupdaterate 66                 // Force 66 updaterate (tickrate) for stability
sv_maxupdaterate 66                 // Match tickrate for proper movement/projectiles
sv_mincmdrate 66                    // Ensure clients send commands every tick
sv_maxcmdrate 66                    // Prevent client desync/delay
// ==== Server CPU / Tick Behavior ====
sv_cacheencodedents 1               // Reduce SendTable_Encode calls; mild CPU savings
tf_resolve_stuck_players 0          // Disable expensive stuck player checks
// ==== Spin / Timers ====
host_timer_spin_ms 0                // Leave at 0; TF2 handles timing better this way
// ==== Network Queueing ====
net_queued_packet_thread 1          // Safe default; prevents unstable multi-threading bugs
net_maxcleartime 0.01               // Hint for packet flush time; negligible but safe
// ==== Packet splitting ====
net_splitrate 500                   // Sensible mid value; avoids packet bursts/choke
// ==== Inter-server / GC ====
sv_master_share_game_socket 1       // Avoid Steam GC issues; safest for TF2 servers
// ==== LZMA / Map loading ====
lzma_persistent_buffer 1            // Keep decompression buffer; tiny improvement on map load

// Download Settings //

//Increases the resizeable spray timeout to the limit
rspr_spraytimeout 999
// Max spray size for resizablesprays
rspr_maxsprayscale 4

// Allow clients to upload customizations files
sv_allowupload 1

// Allow clients to download files
sv_allowdownload 1

// Maximum allowed file size for uploading in MB
net_maxfilesize 15


// Enable timers to wait between rounds. WARNING: Setting this to 0 has been known to cause a bug with setup times lasting 5:20 (5 minutes 20 seconds) on some servers!
mp_enableroundwaittime 0

// Time after round win until round restarts
mp_bonusroundtime 0

// Client CVars //

// Restricts spectator modes for dead players
mp_forcecamera 1

// Toggles whether the server allows spectator mode or not
mp_allowspectators 1

// Toggles footstep sounds
mp_footsteps 1

// Toggles game cheats
sv_cheats 0

// After this many seconds without a message from a client, the client is dropped
sv_timeout 900

// Time (seconds) between decal sprays
decalfrequency 0.1

// Communications //

// enable voice communications
sv_voiceenable 1

// Players can hear all other players, no team restrictions 0=off 1=on
sv_alltalk 1

// Amount of time players can chat after the game is over
mp_chattime 2

//Deals with Idle Players. 1 = Sends them into Spectator mode then kicks them if they're still idle, 2 = Kicks them out of the game
mp_idledealmethod 0
//Maximum time a player is allowed to be idle (in minutes)
mp_idlemaxtime 0
