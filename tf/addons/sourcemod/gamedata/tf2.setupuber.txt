"Games"
{
    "tf"
    {
        "Signatures"
        {
            "CWeaponMedigun::FindAndHealTargets"
            {
                "library"   "server"

                /* Linux and Linux64 C++ symbol. */
                "linux"     "@_ZN14CWeaponMedigun18FindAndHealTargetsEv"
                "linux64"   "@_ZN14CWeaponMedigun18FindAndHealTargetsEv"

                /*
                 * Windows/Windows64 need a fresh byte signature for the current
                 * TF2 server binary.  The first release had a Windows signature
                 * for HealTargetThink(), but that was the wrong function.
                 */
                // "windows"   ""
                // "windows64" ""
            }
        }
    }
}
